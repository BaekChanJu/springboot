<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.dao.MemberDAO">

	<!-- 회원가입 -->
	<insert id="userInsert" parameterType="com.example.domain.MemberVO">
      INSERT INTO MEMBER (userId, userPass, userName) 
      VALUES (#{userId}, #{userPass}, #{userName})

   </insert>
	
<select id="idCheck_Login"  parameterType="com.example.domain.MemberVO" resultType="com.example.domain.MemberVO">


select *
from MEMBER
where userId = #{userId}
	<if test="userPass != null">
	 AND userPass=#{userPass}
	 </if>

</select>

	

</mapper>
